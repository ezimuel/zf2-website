<?php
$this->layout()->disableLayout();
$content = $this->commentForm->render($this);
echo $this->json(array(
    'success' => $this->success,
    'content' => preg_replace('/^\s+/', '', $content),
));
